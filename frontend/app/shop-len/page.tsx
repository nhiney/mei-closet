import { ShopLenClient } from "./ShopLenClient";

export const metadata = {
  title: "🧶 Shop Len - Thời Trang Len Handmade - Mei Closet",
  description: "Bộ sưu tập len thủ công tinh tế — áo len, quần len, váy len và phụ kiện len đan tay.",
};

export default function ShopLenPage() {
  return <ShopLenClient />;
}
